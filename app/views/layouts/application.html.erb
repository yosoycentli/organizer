<!DOCTYPE html>
<html>
  <head>
    <title>Organizer</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= link_to "Cerrar sesión", destroy_user_session_path,  data: {turbo_method: :delete, turbo_confirm: 'Hola, ¿estás seguro de cerrar sesión?'}%>
    <%= yield %>
  </body>
</html>
